apiVersion: v1
kind: Service
metadata:
  name: product-mongo-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    app: product-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
